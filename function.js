// Deklarasi fungsi
function hitungLuasSegitiga(alas, tinggi) {
  // Blok kode fungsi
  return (alas * tinggi) / 2;
}

// Pemanggilan fungsi
const luas = hitungLuasSegitiga(10, 5);
console.log(luas); // 25
